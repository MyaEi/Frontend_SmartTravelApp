export default function SeasonalLayout({ children }: { children: React.ReactNode }) {
  return (
    // <section style={{ padding: "2rem", backgroundColor: "#f9fafb", minHeight: "100vh" }}>
    //   {children}
    // </section>
    <section style={{ padding: "2rem", backgroundColor: "#08254A", minHeight: "100vh" }}>
      {children}
    </section>
  );
}


 
